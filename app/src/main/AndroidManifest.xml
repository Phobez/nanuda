<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nanuda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nanuda">
        <activity android:name=".ExpenseMain"></activity>
        <activity android:name=".EditExpenseActivity" />
        <activity android:name=".CreateNewExpenseActivity" />
        <activity
            android:name=".ExpensesListActivity"
            android:label="@string/title_activity_expense_main_display"
            android:theme="@style/Theme.Nanuda.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>